name: Design system sync

on:
  workflow_dispatch:
  schedule:
    - cron: "23 16 * * 1"

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    uses: JohnnyZLi/Web-Design-System/.github/workflows/consumer-design-system-sync.yml@15d831f437785e22419765c025e7b53abd3a2c61
    with:
      node-version: "24"
      timeout-minutes: 15
      install-command: npm install --no-audit --no-fund --no-package-lock
      validate-command: npm run design-system:integration && npm run lint:html && npm run lint:css
      tracked-paths: |
        design-system.lock.json
        package.json
        assets/design-system
      product-name: portfolio
